class Circle {
  constructor(radius, left, top, duration, color) {
    this.radius = radius;
    this.left = left;
    this.top = top;
    this.duration = duration;
    this.color = color;
  }
}

export default Circle;
